# The kit lives in the LaunchGuard plugin

`kit/` is now part of the `launchguard` plugin: https://github.com/TeemuSo/launchguard-skill

Install it from there:

```
bash "${CLAUDE_PLUGIN_ROOT}/kit/install.sh" [/path/to/repo]
```
